<Window x:Class="CRM.CreateContactItemView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRM"
        mc:Ignorable="d"
        Title="CreateContactItemView" Height="330" Width="200">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MainBtnStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <TextBlock HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="Имя" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Margin="10,31,0,0" TextWrapping="Wrap" Text="{Binding name}" VerticalAlignment="Top" Width="150"/>

        <TextBlock HorizontalAlignment="Left" Margin="10,65,0,0" TextWrapping="Wrap" Text="Номер" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Margin="10,86,0,0" TextWrapping="Wrap" Text="{Binding phone}" VerticalAlignment="Top" Width="150"/>

        <TextBlock HorizontalAlignment="Left" Margin="10,125,0,0" TextWrapping="Wrap"  Text="Компания" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Margin="10,146,0,0" TextWrapping="Wrap"  Text="{Binding company}" VerticalAlignment="Top" Width="150"/>

        <TextBlock HorizontalAlignment="Left" Margin="10,184,0,0" TextWrapping="Wrap" Text="Примечание" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Margin="10,206,0,0" TextWrapping="Wrap" Text="{Binding note}" VerticalAlignment="Top" Width="150"/>

        <Button Content="Добавить"  Command="{Binding AddContactItemtCommand}" Background="#435fe5" Foreground =" white" Style="{StaticResource btn_style}" Height="30" HorizontalAlignment="Left" Margin="10,248,0,0" VerticalAlignment="Top"/>
    </Grid>
</Window>
