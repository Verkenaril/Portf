<Window x:Class="CRM.LoginRegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRM"
        mc:Ignorable="d"
        Title="LoginRegistrationView" Height="450" Width="800">
    <StackPanel>
        <TextBlock Margin="10,10,0,0" Text="Login"/>
        <TextBox HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="{Binding login_input}" VerticalAlignment="Top" Width="120"/>
        <TextBlock Margin="10,10,0,0" Text="Password"/>
        <TextBox HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="{Binding password_input}" VerticalAlignment="Top" Width="120"/>
        <Button Command ="{Binding LoginCommand}" Content="Вход" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
    </StackPanel>
</Window>
